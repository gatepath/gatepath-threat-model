# WireGuard Configuration Examples

This directory contains example WireGuard configurations
for different network paths.

Examples include:
- standard VPN paths (clearnet egress)
- Tor-only VPN paths with no clearnet fallback

These files are examples only and do not include keys.
